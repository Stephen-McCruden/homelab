# FILE PATH: molecule/default/converge.yml
---
- name: Converge node roles suitable for container testing
  hosts: all
  become: true
  gather_facts: true
  roles:
    - role: package_manager
    - role: prereqs
    - role: container_runtime
    - role: kubernetes_node
