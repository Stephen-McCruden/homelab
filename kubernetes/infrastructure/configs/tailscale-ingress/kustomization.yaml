# FILE PATH:
# kubernetes/infrastructure/configs/tailscale-ingress/kustomization.yaml
# PURPOSE: Configure resource-bounded standalone Tailscale ingress proxies.
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - proxyclass.yaml
