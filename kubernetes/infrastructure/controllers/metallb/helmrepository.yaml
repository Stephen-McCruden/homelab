# FILE PATH:
# kubernetes/infrastructure/controllers/metallb/helmrepository.yaml
# PURPOSE: Register the official MetalLB Helm repository.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: metallb
  namespace: flux-system
spec:
  interval: 1h
  url: https://metallb.github.io/metallb
