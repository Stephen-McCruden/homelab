# FILE PATH:
# kubernetes/infrastructure/controllers/longhorn/kustomization.yaml
# PURPOSE: Install Longhorn distributed storage through Flux and Helm.
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
