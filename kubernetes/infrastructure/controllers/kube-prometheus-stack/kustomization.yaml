# FILE PATH:
# kubernetes/infrastructure/controllers/kube-prometheus-stack/kustomization.yaml
# PURPOSE: Install the Prometheus Operator monitoring stack through Flux.
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
